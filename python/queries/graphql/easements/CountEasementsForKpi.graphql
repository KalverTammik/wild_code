query EasementsKpi(
  $first: Int
  $after: String
  $where: QueryEasementsWhereWhereConditions
) {
  easements(
    first: $first
    after: $after
    where: $where
  ) {
    pageInfo {
      currentPage
      total
      hasNextPage
      endCursor
    }
    edges {
      node {
        id
      }
    }
  }
}