query coordinationTypes($first: Int, $after: String) {
  coordinationTypes(first: $first, after: $after) {
    pageInfo {
      hasNextPage
      endCursor
      currentPage
      total
    }
    edges {
      node {
        id
        name
      }
    }
  }
}


