query submissions($first: Int, $after: String, $search: String, $where: QuerySubmissionsWhereWhereConditions, $orderBy: [QuerySubmissionsOrderByOrderByClause!], $trashed: Trashed) {
  submissions(
    first: $first
    after: $after
    search: $search
    where: $where
    orderBy: $orderBy
    trashed: $trashed
  ) {
    pageInfo {
      currentPage
      total
      hasNextPage
      endCursor
    }
    edges {
      node {
        id
        name
        status { id name color type }
        createdAt
        updatedAt
      }
    }
  }
}
