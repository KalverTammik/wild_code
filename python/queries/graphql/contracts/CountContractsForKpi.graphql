query ContractsKpi(
  $first: Int
  $after: String
  $where: QueryContractsWhereWhereConditions
  $whereHas: [ContractsWhereHasInput!]
) {
  contracts(
    first: $first
    after: $after
    where: $where
    whereHas: $whereHas
  ) {
    pageInfo {
      currentPage
      total
      hasNextPage
      endCursor
    }
    edges {
      node {
        id
      }
    }
  }
}
