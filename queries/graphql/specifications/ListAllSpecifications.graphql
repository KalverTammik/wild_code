query specifications($first: Int, $after: String, $search: String, $where: QuerySpecificationsWhereWhereConditions, $orderBy: [QuerySpecificationsOrderByOrderByClause!], $trashed: Trashed) {
  specifications(
    first: $first
    after: $after
    search: $search
    where: $where
    orderBy: $orderBy
    trashed: $trashed
  ) {
    pageInfo {
      currentPage
      total
      hasNextPage
      endCursor
    }
    edges {
      node {
        id
        name
        status { id name color type }
        createdAt
        updatedAt
      }
    }
  }
}
