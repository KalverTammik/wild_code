fragment ProjectFieldsFull on Project {
  id
  name
  number
  status { id name color }
  startAt
  dueAt
  createdAt
  updatedAt
  isPublic
  members {
    edges { node { id displayName deletedAt } isResponsible }
  }
  client { id displayName }
}
