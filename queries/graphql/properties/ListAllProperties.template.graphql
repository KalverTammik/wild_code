query properties($first: Int, $after: String, $search: String, $where: QueryPropertiesWhereWhereConditions, $orderBy: [QueryPropertiesOrderByOrderByClause!], $trashed: Trashed) {
  properties(
    first: $first
    after: $after
    search: $search
    where: $where
    orderBy: $orderBy
    trashed: $trashed
  ) {
    pageInfo {
      currentPage
      total
      hasNextPage
      endCursor
    }
    edges {
      node {
        ...{{EDGE_FRAGMENT}}
      }
    }
  }
}
# import PropertyFieldsMinimal from '../fragments/PropertyFieldsMinimal.graphql'
# import PropertyFieldsFull from '../fragments/PropertyFieldsFull.graphql'
